[package]
name = "kova-core"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Foundational types for Kova: Vector, VectorId, Distance trait."

[dependencies]
thiserror.workspace = true
wide = "0.7"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
proptest = "1"
criterion = "0.5"
bincode = "1"

[[bench]]
name = "distance"
harness = false

[lints]
workspace = true
